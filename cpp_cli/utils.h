#ifndef UTILS_H
#define UTILS_H

#include &lt;string&gt;

class Utils {
public:
    // Trims leading and trailing whitespace from a string
    static std::string trim(const std::string&amp; str);
};

#endif